import 'package:flutter/material.dart';

class BelinhaPage extends StatelessWidget {
  const BelinhaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Belinha')),
      body: const Center(child: Text('Belinha Page')),
    );
  }
}
