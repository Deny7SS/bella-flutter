import 'package:flutter/material.dart';

class ResumosPage extends StatelessWidget {
  const ResumosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Resumos')),
      body: const Center(child: Text('Resumos Page')),
    );
  }
}
